const{test,after}=require("node:test");const assert=require("node:assert/strict");
process.env.DATABASE_URL=process.env.TEST_DATABASE_URL||process.env.DATABASE_URL;
const{pool,ready}=require("./db");after(()=>pool.end());
test("schema katalog PostgreSQL menjaga ownership",async()=>{await ready;await pool.query("TRUNCATE products,stores RESTART IDENTITY CASCADE");const store=(await pool.query("INSERT INTO stores(owner_id,nama,alamat,kategori) VALUES(10,'Toko Uji','Kampus','Makanan') RETURNING *")).rows[0];const product=(await pool.query("INSERT INTO products(owner_id,store_id,nama,kategori,harga,stok,satuan) VALUES(10,$1,'Produk','Makanan',10000,5,'pcs') RETURNING *",[store.id])).rows[0];assert.equal(product.owner_id,10);assert.equal(product.store_id,store.id);await assert.rejects(pool.query("INSERT INTO products(owner_id,store_id,nama,kategori,harga,stok,satuan) VALUES(11,$1,'Terlarang','Makanan',1,1,'pcs')",[store.id]))});
